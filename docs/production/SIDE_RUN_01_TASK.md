# SIDE-RUN-01 — чёткое боковое движение героя

23 сентября 2026. Прямой дефект владельца: бег к камере/от камеры приятный, при движении влево/вправо появляется «мазня».

- Основа: `dev b2f1c8331c7368c77de212342acdd9a19bd679ef`, ветка `codex/side-run-clarity`.
- Результат: боковой бег сохраняет читаемый силуэт и плавное экранное перемещение на ПК и S23, без изменения скорости и управления.
- Исполнитель: Codex — диагностика взаимодействия физики, камеры, покадровой презентации и глубины (D-047), отдельный проход QA. Простую изолированную правку данных при необходимости получает Qwen с отдельным точным контекстом.
- До правки: сравнить четыре направления с живой и остановленной позой, записать экранную опору между отрисованными кадрами при частоте рендера выше физики. Проверить атлас, фазу и выбор ракурса; не считать предположение причиной до воспроизведения.
- Разрешённые игровые файлы по подтверждённой причине: `scripts/courtyard/courtyard_character_visual.gd`, `third_person_camera.gd`, `courtyard_player.gd`, `scripts/world/forest_route.gd`, `assets/shaders/character_depth.gdshader`, `scenes/courtyard/courtyard_player.tscn`, `project.godot`. Только необходимое подмножество; запись при расширении.
- QA/доставка: новый `scripts/tools/validate_side_run.gd/.tscn`, `tools/run-side-run-checks.mjs`, `tools/build-forest-route.mjs`; протокол/сравнения в `docs/production/SIDE_RUN_01_CHECKS.md`, `docs/art/side-run-01/`. MEMORY, STATUS, ROADMAP, README и правила обновляются по D-051.
- Контракт: `CourtyardPlayer._physics_process(delta)` двигает CharacterBody3D через `move_and_slide`; скорости 4.2/6.4. `CourtyardThirdPersonCamera._process(delta)` следует за целью; `set_target`, `snap_to_target`, `reset_view`. `CourtyardCharacterVisual._process` с приоритетом 100 задаёт basis камеры и синхронизирует depth/shadow; `update_visual` сохраняет фазу при смене ракурса. `BackpackLayer` с приоритетом 10 выбирает цельные кадры.
- Визуальный эталон: STYLE_REFERENCE, CHARACTER_VARIANTS, текущие `traveler-run-{side,front,back}-v3.png`, forest-route 0.19.0. Рисунки, палитру и 15 поз/с сохраняем.
- EN/RU и сохранения: новых строк/ID нет, профиль кампании не менять. Исправленная проба обновляет пакет Forest Route; другие приложения сохраняются.
- Приёмка Codex: воспроизводимый провал до правки; измерение плавности после; четыре направления, диагонали, камера при движении, старт/стоп/разворот, столкновение, сброс/телепорт, фокус/пауза; ±рюкзак и ходьба/атака/карман/бой в относящихся регрессиях. Физика и визуальная оценка отдельно. Проверять ошибки в логах и завершение всех групп.
- Платформы: ПК headless и графический Mobile renderer, Android S23 при доступности; сверить package/version/SHA APK. OnePlus не заявлять без проверки. До передачи владельцу установить обычную исправленную пробу и открыть; затем прекратить касания.
- Художественная оценка: кадры и движение в одинаковой сцене; личное принятие владельцем отдельно от QA.
- Границы: без новых механик, арта/UI, изменения боевых окон/скоростей, переписывания персонажа, редактирования PNG или пользовательских `.import`. `main` и пользовательские файлы сохраняются.
- Завершение: протокол → проверенный PR в dev → merge GitHub → fast-forward dev → удалить ветку. Затем отдельная документная BIOME-01D: схема рельефа мира.

## Итог

Два игровых скрипта исправлены Codex. PNG, shader, player controller, сцены и project.godot не изменялись. [12 групп ПК/S23 и регрессии](SIDE_RUN_01_CHECKS.md), установленная обычная 0.19.1 сверена; личная оценка отдельно. Далее BIOME-01D, художественная BIOME-01C отложена по D-051.
