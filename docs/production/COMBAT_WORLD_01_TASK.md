# COMBAT-WORLD-01 — бой в игровом мире

```text
ID и место в очереди: COMBAT-WORLD-01, ревью 2026-09-25 п. 2.3, раздел 4, п. 4; одобрено
  владельцем 25 сентября («давай сразу пошли дальше по плану»).
Для участка мира: планировка не меняется (D-064 не затронут); противник — на существующей
  тропе за сторожевой башней деревни.
Игровой результат (после 01B): на тропе за башней героя встречает противник, герой бьёт,
  держит блок и ловит идеальный блок так же, как в стенде, и видит понятную реакцию.
Основание: бой есть только в стендах scripts/tools; D-051 — механики раньше расширения мира;
  COMBAT_MASTERY.md — замах противника, удержание = блок, своевременное нажатие = идеальный.
Исполнитель: Claude — связи ввода/HUD/боя/мира (D-047).
```

## 01A — боевые компоненты в `scripts/combat/` (ветка `claude/combat-components-01a`)

Перенесены без изменений кода 13 файлов: `fist_defense_controller`, `corner_enemy_actor`, `corner_enemy_visual`, `frame_guard_actor`, `attack_frame_data`, `combat_feedback_fx`, `combat_swing_trails`, `painted_combat_fx`, `painted_combat_trails`, `painted_effect_pool`, `fist_defense_player`, `combat_feedback_player`, `combat_dodge_player`. Стенды, сессии и панели остаются в `scripts/tools/` и используют компоненты по новым путям; пути обновлены в 24 файлах (скрипты, `assets/combat/guard_unarmed_v1.tres`, сборщики, документы).

**Проверено 25 сентября.** 17 боевых тестов на ветке и на чистом dev: коды выхода совпадают; у 6, что падают и на dev, причины те же (сравнение строк ошибок); `fist_sandbox` на dev-копии упал только из-за общей папки языка.

## 01B — противник на тропе в мире (ждёт решения владельца)

У принятого героя D-059 есть стойка, ходьба, бег и удар, **нет поз блока и реакции на попадание**. В стендах эти позы — у прежнего героя; смешивать рисунки D-059 запрещает. Варианты — в STATUS/отчёте владельцу; до решения 01B не начинается.
