# INV-01A — первый доступ к носимым вещам

21 сентября 2026. Ветка `codex/inventory-pocket-menu` от `307bd6b` (`dev`, после PR #19). Версия `0.6.1-inventory-back`, Android код 11. ПК и сценарий S23 проверены; результаты продолжения ниже. Исторические записи 20 сентября сохранены для контекста и заменяются итогом 21 сентября.

## Итог проверки 21 сентября

- На настоящей 0.6.0 обнаружен выход из приложения после одной Back при открытом меню. Диагностическая APK показала два `NOTIFICATION_WM_GO_BACK_REQUEST` в одном кадре (918), между ними события KEY_BACK. Первый закрывал меню, второй вызывал выход. Ollama добавила учёт обработанного кадра; отдельное следующее нажатие продолжает выполнять прежний выход из двора. Никакого временного таймаута нет.
- Независимый `validate_inventory_back.gd`: 6 групп (OPENING/OPEN × двойное уведомление/клавиша первой/уведомление первым), затем отдельный прогон `--expect-closed-exit`. До исправления процесс завершался с кодом 0 посреди первой группы, без итогового маркера: это FAIL. После исправления оба прогона завершились ожидаемыми маркерами без ошибок. Повторены 25 основных групп меню/панели/жеста, графический тест меню с захватом мыши, 8 групп локализации и проверка 63 ключей.
- Обычная APK 0.6.1 установлена на S23: жест до содержимого, окно EN/RU, закрытие кнопкой/Back, Back во время жеста, движение после закрытия, Home и возврат без смены процесса, отдельный последующий Back для выхода, русский язык после перезапуска. При Back из меню PID 1728 сохранился; после второго отдельного Back из двора процесс завершился. Перед финальными касаниями готовность окна проверялась снимком. Ранний повтор после обновления без подтверждённого открытого окна не принят как результат; диагностические пакеты с неполной выборкой сцен также не засчитаны.
- Диагностический `inventory_back_device_probe.gd` — средство QA, в главную сцену и обычные APK/EXE не включён. Финальный пакет снова запускает `first_courtyard.tscn`, содержит только выбранную игровую сцену и зависимости. Логи финального процесса не содержат `BACK_PROBE`, `SCRIPT ERROR` или ошибок Godot. Настройка телефона возвращена к исходному `ru` через меню.
- Экспорт из рабочего каталога выявил блокировку DLL Git-плагина открытым редактором. Эти запуски не считаются чистыми, несмотря на код 0. Итоговые пакеты экспортированы из `.tools/export-staging/inv01-back/`: копия игровых исходников, в копии отключён только автозапуск редакторского Git-плагина. Игровые исходники совпадают с рабочими; копия пресетов и главная сцена восстановлены после диагностики. Оба итоговых экспорта без ошибок; остаются прежние предупреждения FBX и build-tools. EXE проверен запуском, снимком и кодом 0.
- Исходная правка редактора, убравшая явный `locale/fallback="en"`, сохранена: фактический английский резерв подтверждён тестом локализации. Резервная копия до работы: `.tools/checkpoints/resume-20260921/project.godot`. `test_world.tscn` больше не отмечена как несохранённая; редактор не закрывался.

Итоговые пакеты (заменяют хэши 0.6.0 ниже): APK `D5E5CFF4375C9EC4434079385277B51CC8E9884B3FE6C4EB2CAAA552FE5E6B15`; EXE `7D856AE6567D1AB0746C7C655A840F456288006ED07B819C38F0E07BCF4DAB60`. Предыдущие сохранены в `.tools/builds/before-inv01-back-20260921/`.

Свидетельства: `.tools/inv01-back-{before,after,exit}.log`, `.tools/inv01-back-menu-windowed.log`, `.tools/inv01-back-probe-final-log.txt`, `.tools/inv01-back-final-{open,closed}.png`, `.tools/inv01-final-{home,ru,restarted-ru}.png`, `.tools/inv01-final-device.log`, `.tools/inv01-final-windows-confirmed.{log,png}`. OnePlus и длительная приёмка остаются открыты; полный INV-01 не завершён. PR #20 готов к интеграции по этим результатам, `main` не изменялась.

## Объём

Первое окно вещей открывается клавишей **I / Tab** или кнопкой **Inventory / Вещи** на экране. Сначала герой обращается к карману стартовой одежды; содержимое появляется только после завершения рисованного жеста. Повторная команда, Escape, закрытие окна, Android Back, потеря фокуса или доступа к карману отменяют действие и освобождают управление.

Это ограниченный этап INV-01A: просмотр реальных предметов, количества, надетой экипировки и монет. Выбор English / Русский / языка устройства находится внутри окна. Открытие само по себе не сохраняет настройку языка, не выдаёт предметы и не меняет Inventory. Ошибка записи настройки возвращает фактический выбор и показывает сообщение об ошибке настроек; это не уведомление о состоянии здоровья.

Доступ представляет компонент `PocketAccess` стартовой одежды с постоянным идентификатором. При его удалении, замене или отключении меню закрывается. Сам компонент не хранит копии предметов. Размещение вещей между несколькими физическими ёмкостями, смена одежды/рюкзака, ограничение вместимости кармана и запись кампании на диск остаются следующими частями INV-01/SAVE-01. Не считать полный INV-01 завершённым.

В этом прототипе мир двора не ставится на паузу, а ввод героя, камеры и сенсорного HUD временно блокируется. Меню не даёт неуязвимость и не сбрасывает восстановление удара. Нанесение урона врагами во время меню ещё требует проверки вместе с будущей боевой системой. Применение, экипировка из окна, быстрые слоты, карта, здоровье, ранения и магия здесь не добавляются.

## Разделение компонентов и исполнителей

- `courtyard_character_visual.gd` и отдельный `PocketPose`: жест, камера, завершение анимации, восстановление обычного рисунка.
- `pocket_access.gd`: доступ к карману стартовой одежды.
- `courtyard_inventory_menu.gd`: состояния CLOSED → OPENING → OPEN, владение вводом, отмена и потеря доступа.
- `courtyard_inventory_panel.gd`: только чтение снимка Inventory, обновление по событиям, локализованные подписи, настройка языка.
- Редактируемые сцены Godot задают расположение окна; каталоги PO/POT содержат тексты EN/RU. Игровые идентификаторы от языка не зависят.

Весь игровой код, сцены и исправления написал локальный `qwen3.8-24k:latest` через Ollama. Codex подготовил задания, прочитал изменения и самостоятельно написал проверки. Рисунок создан ImageGen по существующему герою; [полный запрос, исходники и разметка](../../art/concepts/traveler-pocket-v1.md). Логи модели находятся в локальной `.tools/ollama-godot/runs/`, в Git не включаются.

Codex также ограничил размер чтения в мосте Ollama: запрос больше 200 строк возвращает ближайшие 200 строк вместо повторяющейся ошибки. Лимиты путей и прав записи не расширены. Это исправляет фактически наблюдавшийся цикл неверных запросов локальной модели.

## Независимая проверка

| Проверка | Результат |
| --- | --- |
| Рисованный жест: 7 групп, включая отмену, паузу, ракурсы, невалидный/зацикленный ресурс | PASS, `ASHBOUND_INVENTORY_GESTURE_OK groups=7` |
| Содержимое и локализация панели: 8 групп, включая владение, неизвестный ID и ошибку записи настройки | PASS, `ASHBOUND_INVENTORY_PANEL_OK groups=8` |
| Сценарий меню: 10 групп, включая ввод, фон приложения, Back, замену кармана и освобождение блокировок | PASS; также графический запуск с настоящим захватом мыши и GUI-кликами, `ASHBOUND_INVENTORY_MENU_OK groups=10` |
| Каталоги: 63 сообщения EN/RU, независимые ожидаемые формулировки | PASS, `ASHBOUND_LOCALIZATION_CATALOGS_OK keys=63 locales=2` |
| Прежние проверки персонажа, камеры, касаний, инвентаря, локализации и учебного маршрута | PASS, 12 запусков; маршрут и сенсорный бег отдельно EN/RU; проверка захваченной мыши — с графическим окном |
| Графическая проверка жеста и окна EN/RU | PASS, пять снимков, текст/границы окна проверены; после просмотра окно сдвинуто правее, чтобы не перекрывать героя |
| Windows и Android экспорт | PASS; EXE запущен, получен снимок основной сцены и маркер готовности без ошибок |
| Samsung S23 — реальная установленная APK | Установка и запуск 0.6.0 подтверждены, стартовая кнопка показана; полный сценарий меню пока НЕ принят |

При разработке проверки выявили неверную сериализацию анимационного ресурса, ошибочное определение завершения по остановке анимации, допуск зацикленного жеста, неверные обращения к API Godot и ошибки обработки касаний. Такие результаты возвращались Ollama для исправления. Выход 0 без ожидаемого маркера всех групп или при `SCRIPT ERROR`/ошибке движка не считается успешной проверкой.

## Воспроизведение

```text
node tools/validate-localization-catalogs.mjs
godot --headless --path . --script res://scripts/tools/validate_inventory_gesture.gd
godot --headless --path . --script res://scripts/tools/validate_inventory_panel.gd
godot --headless --path . --script res://scripts/tools/validate_inventory_menu.gd
godot --path . --script res://scripts/tools/capture_inventory_menu.gd -- --no-mouse-capture
```

Графическая проверка создаёт временные предметы только в отдельном QA-процессе и восстанавливает его исходный снимок. Эти фикстуры не запускаются из основной сцены и не входят в пользовательский маршрут.

## Сборки и ограничения приёмки

Предыдущие пакеты сохранены в `.tools/builds/before-inv01-20260920/`. Текущие пакеты:

| Путь | SHA-256 |
| --- | --- |
| `.tools/builds/windows/AshBound.exe` | `D5585A5784931BD69C8DA430F407BC494527A33171D49E2F6A49AF5CF495EF9D` |
| `.tools/builds/android/ashbound-courtyard.apk` | `7649834FACA452A657BD7D2AED67350BD89505B1739E09592B89DCDFC955C2FD` |

Локальные свидетельства: `.tools/inv01-qa-*.log`, `.tools/inv01-menu-windowed.log`, `.tools/inv01-menu-final.log`, `.tools/inv01-graphical-final.log`, `.tools/inv01-{gesture,empty,items}-*.png`, `.tools/inv01-windows-smoke.png`, `.tools/inv01-export-*.log`. Проверены ожидаемые маркеры и отсутствие ошибок. Импорт сохраняет прежние предупреждения старой FBX-сцены и её volumetric fog; активный двор их не использует. Экспорт Android сохраняет известное предупреждение выбора build-tools 30.0.3.

Первоначальный запуск `validate_target_input.gd` без графического вывода не прошёл: такой режим не обеспечивает захват мыши. Неизменённый тест затем пройден в обычном графическом окне; ошибка способа запуска не маскируется ослаблением условий. Новый тест меню также отдельно проверен с графическим выводом, восстановлением захвата и реальными GUI-событиями мыши.

На S23 во время прогона независимо от действий Codex изменились язык на русский и положение камеры, затем экран уснул. Отправлен вопрос, не идёт ли ручная проверка владельца; дальнейшие касания приостановлены, чтобы не вмешиваться. Это не доказательство ни успеха, ни дефекта меню на устройстве. Появившийся `settings.cfg` не удалён и не перезаписан. OnePlus не подключён, на нём новая версия не проверялась.

До слияния нужно закончить на свободном S23: открытие после жеста, закрытие, выбор EN/RU и его сохранение после перезапуска, Back, сворачивание/возврат и движение после закрытия. Полная пользовательская приёмка оформления и будущие рюкзаки/действия с вещами остаются отдельно. `main` не изменялась.

[Черновик PR #20](https://github.com/AlekseyChist/ashbound/pull/20) опубликован в dev. Конфликтов нет, серверные проверки не настроены; основание проверки — перечисленные локальные прогоны. Ветка остаётся активной до завершения S23, main не изменена. Для владельца запущено отдельное видимое окно Godot на ПК; несохранённый редактор сохранён открытым.
